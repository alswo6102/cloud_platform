---
name: entity-resolve
description: Resolve a user-supplied project, service, or framework name against live CLI data. Returns exact matches or evidence-backed similar candidates without confirming them.
---

# Entity Resolve

Use the live project, service, or framework catalog. Exact matches may be used
directly. Similar matches are candidates only and require explicit user
confirmation. Never invent a candidate and never promote a candidate to an
execution argument.
