---
name: service-status
description: Inspect configured ports, frontend labels, Docker state, health, restart count, and published ports for one service or an entire project.
---

# Service Status

Require a valid `project`. Accept an optional `service`.

Verification: Join Compose configuration with Docker containers using Compose labels.
